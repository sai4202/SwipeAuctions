# Copy to deploy/.env on the VPS (never commit the real file — it's gitignored).
# DB_USER/DB_PASSWORD are new credentials for the self-hosted Postgres — not the old Supabase ones.
DB_USER=swipeauctions
DB_PASSWORD=

# Everything below: same values currently backing the Railway deployment.
JWT_SECRET=
MAIL_USERNAME=
MAIL_PASSWORD=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
RAZORPAY_ACCOUNT_NUMBER=
SWIPE_STOCK_SELLER_PASSWORD=
